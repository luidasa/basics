  <form action="{{ path_for('auth.password.update', {'id': id, 'code': code }) }}" method="post" role="form">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
          <div class="login-panel panel panel-default">
              <div class="panel-heading">
                  <div class="row">
                      <div class="col-lg-6">
                          <h3 class="panel-title">Cambiar contraseña</h3>
                      </div>
                      <div class="col-lg-6 text-right">
                          <a href="{{ path_for('auth.signup') }}"> Registro</a>
                      </div>
                  </div>
              </div>
              <div class="panel-body">
                  <fieldset>
                      <div class="form-group input-group">
                          <div class="input-group-addon"><i class="fa fa-lock"></i></div>
                          <input class="form-control" placeholder="Nueva contraseña" name="password" type="password" autofocus>
                      </div>
                      <div class="form-group input-group">
                          <div class="input-group-addon"><i class="fa fa-lock"></i></div>
                          <input class="form-control" placeholder="Confirmar contraseña" name="repetir" type="password">
                      </div>
                  </fieldset>
              </div>
              <div class="panel-footer">
                  <div class="row">
                      <div class="col-lg-6">
                          <!-- Change this to a button or input when using this as a form -->
                          <button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Enviar</a>
                          </div>
                          <div class="col-lg-6 text-right">
                              <a href="{{ path_for('auth.signin') }}">Iniciar sesión</a>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
        </div>
      </form>
